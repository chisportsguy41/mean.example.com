extends ../layout

block content
  h1 The Blog
  for post in posts

    div.article
      a(href='posts/' +   post.slug)
        h2= post.title
        div!= post.description

  script(src='/dist/js/posts.app.min.js')
